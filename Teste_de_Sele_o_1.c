#include <stdio.h>
#include <math.h>

int main()
{

    int A, B, C, D;

    scanf("%d", &A);
    scanf("%d", &B);
    scanf("%d", &C);
    scanf("%d", &D);

    somaAeB = A + B;
    somaCeD = C + D;

    if (B > C && D > A && somaCeD > somaAeB && (C > 0 && D > 0) && par != 0)    
    {
        printf("Valores aceitos\n");
    }
    else
    {
        printf("Valores nao aceitos\n");
    }
    return 0;
}